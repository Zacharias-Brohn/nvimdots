require("colorizer").setup({
    user_default_options = {
        mode = "virtualtext",
        css = true,
        tailwind = true,
        sass = { enable = true, parsers = { "css" }},
        virtualtext_inline = 'before',
    },
})
